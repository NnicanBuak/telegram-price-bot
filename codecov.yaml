codecov:
  require_ci_to_pass: no
  max_report_age: off

coverage:
  precision: 2
  round: down
  range: "70...100"

comment:
  layout: "reach, diff, files"
  behavior: default
  require_changes: false

flags:
  unittests:
    carryforward: true
